candidate_horizons_hours = [1, 6, 12, 24, 48]
fixed_clock_selected_horizons_hours = [1]
scheduled_event_start_selected_horizons_hours = [1, 6, 12]
main_staleness_minutes = 15
robustness_staleness_minutes = 60
early_settlement_tolerance_minutes = 15
candlestick_interval_minutes = 1
candlestick_lookback_hours = 24
batch_size = 100

[study_window]
analysis_anchor_start_utc = "2025-07-01T00:00:00Z"
analysis_anchor_end_utc_exclusive = "2026-07-01T00:00:00Z"
allowed_timing_structures = [
  "fixed_clock",
  "scheduled_event_start",
]
allowed_binary_results = [
  "yes",
  "no",
]

[anchor_verification]
api_occurrence_datetime_is_verified_by_default = false
event_strike_date_is_verified_by_default = false
close_time_may_be_anchor = false
expiration_time_may_be_anchor = false
settlement_time_may_be_anchor = false

[kalshi_metadata]
page_size = 1000
max_retries = 5
backoff_base_seconds = 1.0
backoff_cap_seconds = 30.0
requests_per_second = 3.0
timeout_seconds = 45.0
mve_filter = "exclude"

[kalshi_partitioned_acquisition]
partition_pages = 25
max_raw_bytes = 5368709120
min_free_bytes = 85899345920
estimated_compressed_page_bytes = 16777216

[kalshi_event_metadata]
page_size = 200
batch_size = 200
partition_events = 5000
max_pages_per_batch = 10
max_retries = 5
backoff_base_seconds = 1.0
backoff_cap_seconds = 30.0
timeout_seconds = 45.0
requests_per_second = 3.0
estimated_compressed_raw_bytes_per_event = 1536
estimated_compressed_normalized_bytes_per_event = 512
estimated_single_event_fallback_fraction = 0.03
estimated_fallback_raw_bytes_per_event = 4096
estimated_milestone_pages_per_fallback = 1
max_raw_bytes = 5368709120
min_free_bytes = 85899345920
